pipeline {
    agent none
    
    stages {
        stage('Check build') {
            agent {label 'aft-nuc'}
            when {branch 'PR-*'}
            steps {
                sh './jenkins/ci.sh'
            }
        }
    }
}